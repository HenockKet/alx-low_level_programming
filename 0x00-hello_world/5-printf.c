#include<stdio.h>
/**
 * main - doesn't return any value
 * Return: 0 after the text is printed
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
