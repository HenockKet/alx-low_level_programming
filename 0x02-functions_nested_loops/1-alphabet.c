#include "main.h"
/**
 * print_alphabet - print alphabet
 * Return:  has no return value
*/
void print_alphabet(void)
{
char ch;
ch = 'a';
for (ch = 'a'; ch <= 'z'; ch++)
_putchar(ch);
_putchar('\n');
}
