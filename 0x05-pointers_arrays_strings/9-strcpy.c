#include "string.h"
/**
 * *_strcpy - copy string form src to dest
 * @src: given sting to be copied
 * @dest: copied string
 * Return: dest/copied string
 */
char *_strcpy(char *dest, char *src)
{
strcpy(dest, src);
return (dest);
}
